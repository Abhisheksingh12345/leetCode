#include <iostream>
#include <vector>
using namespace std;

class Solution
{
public:
    int plusOne(vector<int> &nums)
    {

        
    }
};

    int main()
    {
        Solution s;
        int myints[] = {1,2,3};
        std::vector<int> nums(myints, myints + sizeof(myints) / sizeof(int));
        cout << s.plusOne(nums);
        return 0;
    }